import * as Alliance from "@earnalliance/node";

// let alliance;

export const getAllianceClient = () => {
  // if (!alliance) {
  //   alliance = Alliance.init();
  // }
  // return alliance;
  return Alliance.init();
};
